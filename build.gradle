plugins {
    id 'java'
    id 'application'
}

group 'com.merge.sort'
version '1.0-SNAPSHOT'
mainClassName='com.merge.sort.App'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.6.0'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine'
}

test {
    useJUnitPlatform()
}